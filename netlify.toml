[build]
  command = "npm run build"
  publish = "dist"

# Ensure client-side routing works (React + Vite)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# (Optional) Set security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
